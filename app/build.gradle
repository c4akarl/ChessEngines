apply plugin: 'com.android.application'
android {
    compileSdkVersion 29
    buildToolsVersion "28.0.3"

    defaultConfig {
        applicationId "ccc.chess.engines"
        minSdkVersion 14
        targetSdkVersion 29
        versionCode 14
        versionName "2.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}